[project]
name = "wordle-in-one"
version = "0.1.0"
description = "A mobile-first Wordle puzzle game"
requires-python = ">=3.10"
dependencies = ["Flask==3.1.3", "gunicorn==23.0.0"]

[tool.uv]
package = false
